
function sem1(num) {
  var branch = document.getElementById('sBranch').value;
  changeUrl(branch, num)
}
let url;
function changeUrl(branch, num){
const iframe = document.getElementById('dynamicIframe')

if (branch == computer-science && num == 1) {
  url = 'www.google.com';
}

iframe.href = url;
}